from data import Planet, Player
import math
from typing import List

import logging

# logging
logger = logging.getLogger("sbc")

def movement_phase(orders):
    """
    Handle movement phase for each player
    each order is executed sequentially
    """

def jump(player: Player, arguments: List[str]):
    print("jumping to the infinity and beyond ! Method has to be written first :) ")

